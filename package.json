{
  "name": "modular-video-player",
  "version": "1.0.0",
  "description": "Modular video player for Vercel",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "build": "mkdir -p public && cp index.html public/ && cp -r css js public/ && cp videos.json public/"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  }
}